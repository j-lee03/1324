CREATE TABLE interview_sessions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    role_query VARCHAR(255) NOT NULL COMMENT '사용자가 입력한 직무',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '세션 생성 시간',
    INDEX idx_role_query (role_query)
) COMMENT='면접 세션 관리 테이블';